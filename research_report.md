# CrossFM Phase 0/1 research report

Status: awaiting Kaggle pilot results. This file will be completed from validated artifacts only; queued, running, partial, or failed outputs are not evidence.

## Hypothesis and benchmark

The immediate gate tests the prerequisite that semantic and statistical backbones have complementary failure modes. Regime C uses six paired feature mechanisms. In context, five auxiliary pair differences are highly correlated with the stable premium–income difference; out of context they decorrelate. Metadata identifies the stable pair, while labels identify the episode-specific direction.

## Architecture and baselines

No CrossFM architecture is implemented. The baselines are frozen Qwen3-0.6B prompted with task/schema/examples, frozen TabICL v2 without metadata, and a declared non-learned diagnostic oracle. The oracle is only an identifiability check and must not be interpreted as a model result.

## Results, failures, cost, and recommendation

To be populated after a complete run passes artifact validation.

