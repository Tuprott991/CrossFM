# Research log

| Observation | Hypothesis | Experiment | Result | Interpretation | Next experiment |
|---|---|---|---|---|---|
| TabICL v2 documents pretraining on 300–48k rows. | Its inductive bias should dominate Qwen on the 384-row anonymized nonlinear regime but may extrapolate poorly to 10–28 rows. | Frozen Phase 1 A/B/C pilot. | Pending. | Pending. | Run engineering smoke, then frozen exploratory pilot only if smoke is complete. |
| V1 Qwen3-0.6B predicted class 1 on under 1% of queries; sequence likelihood and LOW/HIGH verbalizers also collapsed locally. | The checkpoint/interface is too weak for a credible semantic baseline. | Test bounded generation and the next protocol-approved model size. | Qwen2.5-1.5B follows strict `FINAL: LOW/HIGH`; redesigned A reached 97.5% on a 40-query local gate. | V1 cannot test semantic complementarity; v2 is a new exploratory protocol, not a confirmatory retry. | Run private v2 engineering smoke, then the frozen v2 pilot if all artifact gates pass. |
| Phase 1 v2 completed 27/27 tasks and passed A/B/C complementarity gates. | Strong one-way and tool baselines may explain C without recurrent communication. | Phase 2 B0--B6 with disjoint aliases and held-out C interaction forms. | Pending. | Pending; Phase 2 is exploratory. | Run private engineering smoke, then freeze and run the full Phase 2 baseline grid only if smoke validates. |
| Phase 2 full v1 failed after 49/63 task records when one B5 response began an explanation and exhausted the generation cap before its label. | Exact sequence-level verbalizer likelihood removes free-form parser failures and evaluates multi-token labels correctly. | New exploratory v2 protocol; rerun smoke before full. | Pending. | V1 partial outputs are invalid scientific evidence and remain preserved for diagnosis only. | Validate v2 likelihood scoring end-to-end, then submit a new full kernel slug. |
| Phase 2 v2 smoke OOMed before evaluation because full-vocabulary logits and log-softmax were materialized for every long-prompt position. | Applying the frozen LM head only at candidate-suffix positions is mathematically identical and memory-safe. | New exploratory v3 smoke with position-selective sequence likelihood. | Smoke completed 42/42 tasks at 8.99 GB peak T4 memory. | V2 produced no usable task records; the v3 implementation passed its engineering gate. | Run the separately frozen full v3 protocol. |
| Phase 2 v3 completed 63/63 tasks with all hashes valid. | If recurrent communication is needed, strong one-way and compute-matched controls should remain weak on C. | B0--B6 on disjoint aliases and held-out C product mechanisms. | One-way LLM-to-TFM reached 0.8708 on C versus 0.5813 TFM, 0.5208 LLM, and 0.5687 ensemble; the three-call control was also 0.8708. | The present C benchmark is solved by one-way semantic feature-view selection and cannot test the necessity of recurrent co-reasoning. | Stop before Phase 3; design a C regime requiring evidence-dependent second queries, then assign a new exploratory protocol ID. |
| Current C is solved by one-shot semantic pair selection. | A 16-branch code requiring statistical routing before semantic pair selection should defeat fixed one-way queries. | Phase 2.5 C2 with held-out aliases, codebook permutations, and transformations; B0--B6 plus adaptive diagnostic. | Pending Kaggle smoke. | Local numerical-only controls are near chance and the adaptive diagnostic reaches 0.93. | Freeze and run private T4x2 smoke before the full exploratory gate. |
| Forcing every episode through a TFM-to-LLM adapter can erase the strong semantic prior in A; hard sign bits also discard routing uncertainty. | An exact residual bypass should preserve A, and a continuous 16-state posterior should be more robust than an argmax route. | Phase 2.75 matched hard-versus-soft routed adapters with a shared evidence-strength gate and the same Phase 2.5 splits. | Pending private T4x2 smoke after local gates. | Separate exploratory protocol; no result may be inferred from the running Phase 2.5 job. | Validate zero-evidence identity, parameter budget, artifact schema, then run smoke and full comparison. |
| Phase 2.75 completed 90/90 tasks with valid hashes. | Residual soft routing may preserve A without independently solving C2. | Three-seed full run on the exact Phase 2.5 splits. | Soft residual: A 1.0000, B 0.9167, C2 0.5000; hard residual C2 0.5083; diagnostic C2 0.9222. | Preservation succeeds, hard discretization is unnecessary, and a large complementary gap remains. Phase 3 is justified but recurrence must beat a matched one-round model. | Implement cached CrossFM-1/CrossFM-R overfit smoke with exact bypass, continuous messages, and frozen backbones. |
| Phase 3 smoke v1 completed all 48 tasks but failed the overfit gate. | Compressing a 17-view response bank to a single probability discards the rich evidence required by the bridge. | Two-round cached CrossFM on 100 C2 training episodes. | Train accuracy 0.6475, despite nonzero gradients and a 0.1665 mean absolute zero-message effect; peak VRAM 6.57 GB. | Optimization is active, but the evidence bottleneck is structurally too narrow. No full run is allowed. | New v2 protocol: retain prediction-conditioned per-view responses in each latent update, expand the bridge to 256 hidden units, and rerun overfit smoke with cache-ceiling diagnostics. |
| Phase 3 smoke v2 retained structured view responses but still failed its absolute overfit gate. | The remaining failure may be route-to-view binding rather than missing statistical evidence. | 300-epoch, 2.37M-parameter bridge plus response-bank ceiling diagnostics. | Train accuracy 0.7088; relevant-view bank accuracy 0.8975 and post-hoc best-view accuracy 0.9175. | The cache contains sufficient signal, but the bridge does not bind the posterior to the disclosed codebook reliably. The 0.95 gate also exceeds the observable frozen-bank ceiling under stochastic labels. | New v3 protocol: expose the continuous posterior mapped through the disclosed codebook as a round-2 attention prior, preserve B through its specialist residual, and require performance within 0.05 of the response-bank ceiling. |
| Phase 3 smoke v3 completed 48/48 tasks and passed every predeclared engineering gate. | A round-2-only continuous route-to-view prior should restore binding while leaving the one-round comparator blind to the route. | Two replicated T4 workers, 100 C2 training episodes, exact A/B preservation paths, and the same cached response bank as v2. | Two-round train accuracy 0.9600 versus 0.8975 relevant-view accuracy; test C2 rose from 0.4375 at one round to 0.8750 at two, while zero-message C2 was 0.40625; A and B were both 1.0000. | The route binding works and recurrence is necessary in this smoke sample. The shuffle result is not informative because each rank has one C2 test episode per seed, making the within-shard roll an identity. | Proceed to the frozen exploratory full v3 protocol, where 7--8 C2 episodes per rank make the shuffle ablation non-degenerate. |
| Phase 3 full v3 terminated during hardware preflight with zero tasks executed. | The runtime validator assumed Kaggle's device name retained the vendor prefix. | Compared the frozen `NVIDIA Tesla T4` requirement to Kaggle's reported `Tesla T4`. | Immediate `RuntimeError`; no models loaded and no scientific result produced. | This is an execution-only name-normalization defect, not evidence for or against CrossFM. | Normalize optional GPU vendor tokens and rerun under a new exploratory full protocol ID. |
| The Vast dynamic-transfer smoke changed the architecture when the intended comparison was an optimized execution of the cached-response protocol. | Its result is out of scope for the frozen cached CrossFM-R research question. | Preserve raw artifacts for audit but exclude the run from scientific conclusions and all forward protocol decisions. | Withdrawn from the evidence chain. | No inference about cached CrossFM-R is permitted from this run. | Continue only with the cached-response architecture unless a separately motivated protocol is proposed. |
| Phase 3 full v4 completed 72/72 tasks and passed every test-time recurrence, preservation, and message-ablation check; its validation-selected checkpoint missed the absolute training-accuracy gate by 0.003125. | A third round should be useful only if it performs uncertainty- and residual-aware correction rather than repeating soft attention. | New Phase 4 protocol with a shared three-round model, five untouched test seeds, 100 episodes per seed, one immutable backbone cache, and ten total methods covering depth, direction, shuffle/zero, stop-gradient, compute matching, and a random bridge. | Pending engineering smoke. | Exploratory only; the Phase 3 formal gate miss remains recorded and Phase 4 cannot retroactively convert it into a pass. | Validate the corrective round and artifact grid locally, then launch a private T4x2 smoke before the full run. |
